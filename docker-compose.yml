version: '3.8'

services:
  jekyll:
    build:
      context: .
      target: build
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
      - /srv/jekyll/_site
      - /srv/jekyll/.sass-cache
      - /srv/jekyll/.jekyll-cache
    command: jekyll serve --host 0.0.0.0 --port 4000